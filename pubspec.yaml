name: fighthub
description: "MMA Prediction and Analysis Platform"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  dio: ^5.3.2
  sqflite: ^2.3.0
  path: ^1.8.3
  json_annotation: ^4.8.1
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  json_serializable: ^6.7.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  
  assets:
    - assets/data/

  # The following code block is commented out as per the original file
  # flutter:
  #   uses-material-design: true 